# Local development only. Copy to .env and fill in — never commit a populated .env file.
# On Streamlit Community Cloud, set these in the app's Settings > Secrets panel instead
# (see .streamlit/secrets.toml.example for the format Secrets expects).
SUPABASE_URL=
SUPABASE_ANON_KEY=
